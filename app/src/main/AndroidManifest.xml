<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hifly.ac.kr.attention">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
      <!--  <service android:name=".MessageService">
            <intent-filter>
                <action android:name="hifly.ac.kr.attention.message_service"/>
            </intent-filter>
        </service>-->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main_Friend_Info_Activity">
        </activity>
        <activity android:name=".Main_Friend_Call_Activity">
        </activity>
        <activity android:name=".Main_Friend_Message_Activity">
        </activity>
        <activity android:name=".VoiceTest"/>
        <service android:name=".VoiceService"/>

    </application>

</manifest>